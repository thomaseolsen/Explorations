using LList;

namespace Main {
    class Program {
        static void Main(string[] args) {
            var list = new LinkedList<int>();
            System.Console.WriteLine(list.length());
        }
    }
}