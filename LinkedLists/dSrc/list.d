import std.stdio;

// struct Node(T) {
//     public:
//         T data;
//         Node next;
//         Node prev;
// }

class List(T) {
    private:
        int length;
        // Node head;
        // Node tail;
        // Node curr;

    public:
        int Length() {
            return 0;
        };

        void foo() {
            writeln("bar");
        }
}

